en:
  theme_metadata:
    description: "Automatically casts a vote for users on topics they create in voting-enabled categories."
    settings:
      auto_vote_enabled: "Enable automatic voting on user's own newly created topics"
      auto_vote_categories: "Categories where auto-voting is enabled (leave empty for all voting-enabled categories)"
      auto_vote_debug_mode: "Enable console logging for debugging auto-vote behavior"
